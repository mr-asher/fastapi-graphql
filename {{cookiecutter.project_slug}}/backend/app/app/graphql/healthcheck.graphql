directive @needsPermission on FIELD_DEFINITION

type Query {
  healthCheck: Healthcheck 
  checkAuthToken: CheckAuthToken
}

type Healthcheck {
  ok: Boolean!
}

type CheckAuthToken {
  ok: Boolean! @needsPermission 
}
